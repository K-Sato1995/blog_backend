<%= form_for @post do |f|%>
<%= f.text_field :title %>
<%= f.text_area :context %>
<%= f.select(:status, Post.statuses.keys) %>
<%= options_for_select([['Draft', 0], ['Published', 1], ['Private', 2]]) %>
<%= f.submit 'Create a new Post'%>
<% end %>
