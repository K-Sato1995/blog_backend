{
  "name": "MyBlog",
  "private": true,
  "dependencies": {},
  "scripts": {
    "start": "PORT=8000 react-scripts start",
    "setup": "docker-compose run app rake db:setup",
    "up": "docker-compose up",
    "build": "docker-compose build",
    "console": "docker-compose run app rails c",
    "spec": "docker-compose run -e 'RAILS_ENV=test' app rspec"
  }
}
